---
Configuration:
  status: warn

  appenders:
    Console:
      name: LogToConsole

  Loggers:
    logger:
      - name: homework32
        level: debug
        additivity: false
        AppenderRef:
          - ref: LogToConsole


    Root:
      level: error
      AppenderRef:
        ref: LogToConsole
...